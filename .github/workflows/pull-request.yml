name: Pull Request

on:
  pull_request:
    branches: [ main ]

jobs:
  lint:
    uses: ./.github/workflows/python-lint.yml
  tests:
    needs: [ lint ]
    uses: ./.github/workflows/python-package.yml
